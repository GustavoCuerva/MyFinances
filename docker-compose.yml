services:
  myfinances.presentation:
    image: ${DOCKER_REGISTRY-}myfinancespresentation
    build:
      context: .
      dockerfile: MyFinances.Presentation/Dockerfile
    ports:
      - 8080:8080
      - 8081:8081
